# === Research Pipeline (research/cli.py) ===

# OpenAI — default provider for script analysis
OPENAI_API_KEY=sk-...

# Anthropic — alternative provider (use --provider anthropic)
ANTHROPIC_API_KEY=sk-ant-...

# === GEM Video Pipeline (src/cli.ts) ===

# Higgsfield credentials for image & video generation
HF_CREDENTIALS=your_key_id:your_key_secret
